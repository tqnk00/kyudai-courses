# 授業さがし 3巡目レビュー（自己レビュー）

対象は 2026-09-22 時点の `syllabus-db-fixed/` と `app-fixed/kyudai-courses-2026.html`。
2巡目レビュー（REVIEW-2.md）以降に自分が足した部分——文学部の取り込み、各学部の
時間割表からの教室、基幹教育B表の事前申請、時間割ページ——を中心に見直した。

規模: Python 9ファイル 1,916行 + 新規4ファイル 581行、テンプレート1,632行、
テスト36件。サイトは2,647科目 / 7.58MB（gzip 1.5MB）。

---

## 対応状況（2026-09-22 追記）

A1〜A6 と B1・B2 を直した。B3・B4 と C は手つかず。

| | 内容 | 対応 |
|---|---|---|
| A1 | 循環依存 | `timetable_rooms.py` が科目名をDBから引くようにした。絶対パスも撤去 |
| A2 | 入力が出力先に同居 | `config.DATA_DIR`（既定 `data/`）を作り、取り込み4ファイルとPDFを移した |
| A3 | 黙って機能が消える | `load_extra()` で欠品を `!!` 付きで知らせ、作り直すコマンドも出す |
| A4 | 位置引数の配列 | 詳細行を `{label, value, num, html}` のオブジェクトにした |
| A5 | 作り直すジョブが無い | `python run.py rooms`（`--refresh` で取り直し）を追加 |
| A6 | `n_room` が実態と違う | 教室を持つ科目の総数に直した（1,397件） |
| B1 | 新規モジュールが素通し | `tests/test_timetable.py` を追加。テストは36→49件 |
| B2 | 捨てた件数が残らない | 教室JSONの `_meta.per_faculty` に検出・確定・除外を書くようにした |

`run.py rooms` は19本のPDFをウェブから取り直してJSONまで作り直せることを確認した。
その過程で、経済学部のURLが200を返しながらHTMLだったため、手元の正しいPDFを
壊しかけた。`%PDF` で始まるか確かめてから保存するようにしてある。

---

## A. 確かな欠陥

### A1. `timetable_rooms.py` が自分の出力を入力にしている（循環依存）

`timetable_rooms.py:17` は `app-fixed/site-data-2026.json` を読んで科目名の一覧を作り、
それと時間割PDFを突き合わせて `timetable-rooms-2026.json` を出す。その出力を
`site_data.py:222` が読んで `site-data-2026.json` を作る。**出力が入力になっている。**

- 更地からはビルドできない。先にサイトを作らないと教室が作れない
- 科目名が変わったとき、古いサイトJSONを見て照合するので静かにずれる
- パスが絶対パスの直書きで、この1台でしか動かない（`timetable_econ.py:9` はさらに
  セッション固有の作業ディレクトリを指している）

科目名の一覧はDBから直接引けばよく、サイトJSONを経由する必要がない。

### A2. 取り込み用のJSONが出力ディレクトリに同居している

`app-fixed/` に、生成物（`site-data-2026.json`、HTML）と手で置いた入力
（`lit-courses-2026.json`、`core-b-2026.json`、`timetable-rooms-2026.json`、
`timetable-missing-2026.json`）が混ざっている。出力を消すつもりで掃除すると入力も
消え、しかも消えたことに気づけない（A3）。入力は `data/` のような別の場所に置き、
出力ディレクトリは毎回作り直せる状態にすべき。

### A3. 取り込みファイルが無いと、機能が黙って消える

`site_data.py` の4箇所がすべて `if path.exists():` で、無ければ何も言わずに進む。
`core-b-2026.json` を置き忘れれば事前申請155件と教室789件が消え、
`lit-courses-2026.json` を置き忘れれば文学部256科目がまるごと消えるが、
ログにも画面にも出ない。**現に、この2つは私が手でコピーする運用になっている。**
「あるはずのものが無い」は警告すべき。少なくとも件数0を明示したい。

### A4. 詳細行が位置引数の配列で、意味が読めない

`["教室", room, false, true]` の3番目は「数字として右寄せ」、4番目は「生HTMLとして
入れる」。この形にしてから**自分で2回間違えた**。

1. 出典リンクを足したとき4番目を付け忘れ、リンクのHTMLがそのまま文字で出た
2. 直したあと、`room` を自分でエスケープしたのに4番目を `!!c.rmu` にしていたため、
   文学部の教室だけ二重エスケープされる状態になった（今回修正済み）

`{label, value, num, html}` のオブジェクトにすれば、どちらも起きない。

### A5. `run.py` に教室・B表を作り直すジョブが無い

`daily` / `weekly` を回しても、教室と事前申請は古いJSONのまま使われる。学部の時間割表は
学期中も更新される（法学部は9/7、芸工は9/16、B表は9/15が最新）のに、取り直す手順が
コードのどこにも無く、人の記憶に依存している。`run.py rooms` のようなジョブが要る。

### A6. `meta.n_room` の数が実際と違う

`site_data.py` は `n_room + n_core` を入れているが、これは「PDFから足した数」であって
教室を持つ科目の総数ではない（文学部の240件が入らない）。実際は 1,151 対 1,391。
画面では使っていないが、確認のたびに数が合わずに混乱する。名前が実態と違う。

---

## B. 危ういところ

### B1. 新しく足したモジュールにテストが1件もない

36件のテストは `extract` / `db` / `grading` が中心で、`lit.py`（339行）、
`timetable_rooms.py`（306行）、`core_btable.py`（145行）は素通し。

今回B表で見つけた3つの取りこぼしは、いずれも小さな入力を1本テストしていれば
すぐ気づけた類だった。

- 2026年度版は `〇`（漢数字のゼロ）、2025年度版は `○`（白丸）で字が違う → 180件取りこぼし
- 印が `●※` と重なる行を、完全一致で判定していて落としていた
- 表の升が空になる行があり、講義コードを取れていなかった（教育学特論）

PDFそのものは置けなくても、抜き出した数行のテキストを固定データにすれば書ける。

### B2. 名前で照合する方式は、取りこぼしが見えない

教室の突き合わせは「同名の科目がちょうど1件のときだけ採る」安全策にしてあるが、
2件以上あったものは黙って捨てている（経済1件、工学3件）。安全側に倒すのは正しいが、
**捨てた件数がどこにも残らない**ので、あとで詰める手がかりにならない。
講義コードで引ける基幹教育B表と農学部にはこの問題が無く、そちらが本来の形。

### B3. `--set all` という名前が、用途と合っていない

このツールは後期・通年が対象で、既定の `phase1` がそれにあたる。`all` は前期系8区分も
含むため、一覧が4,258→8,502件に倍増し、終わった前期の科目まで取りに行く。
**今回これを踏んで、途中で止めてDBを戻した。** `all` が要るのは調査のときだけなので、
名前を変えるか、既定以外を選んだら何が増えるかを実行前に出したほうがいい。

### B4. `site_template.html` に全部入っている

1,632行の中に、データ整形・絞り込み・時間割描画・詳細描画・URL同期・localStorage・
時間割ページが同居している。単一HTMLで配る方針から来ているので分割は難しいが、
少なくとも描画とそれ以外は関数の並びで分けたい。今は `rows` の組み立てが
3箇所に散っている。

---

## C. 好みの範囲

### C1. 埋め込むJSONの半分が授業計画

`courses` の3.47MBのうち `pl`（授業計画）が1.74MBで50%、`d`（概要）が17%。
検索と時間割だけなら要らない。詳細を開いたときに取りに行く形にすれば初回が軽くなる。
ただし単一HTMLで配れる利点は失われるので、今のままでも筋は通っている。

`rmu`（教室の出典URL）は同じ文字列を1,151回書いていて0.06MB。`meta` に一覧を置いて
番号で引けば消える。

### C2. 未掲載科目の一覧が常に全学部ぶん出る

法学部で絞り込んでいるときは、法学部の52件だけ出すほうが読みやすい。

### C3. 事前申請の締切がアプリに無い

前期は4/1正午〜4/3。後期ぶんはPDFに無く、Moodleでの告知になっている。
締切そのものが本体なので、せめて「Moodleで日程を確認」への導線は要る。

---

## まとめ

2巡目で指摘した9件の欠陥は直っているが、**そのあと自分が足した部分で新しい借金を
作っている。** 中心は A1〜A3 で、要するに「外部PDFから取ったデータの置き場と作り直し
方が決まっていない」こと。いまは私が手でコピーして回しており、半年後の本人には
再現できない。

直す順番としては、

1. A3（黙って消えるのをやめる）— 数行で済み、いちばん怖い事故を防ぐ
2. A1 + A2（入力の置き場を決め、DBから名前を引く）— 循環依存を切る
3. A5（`run.py rooms`）— 更新手順をコードに載せる
4. B1（新規モジュールのテスト）— 2〜3件でいい

A4は今回直したが、配列のままなので同じ間違いは再発しうる。
